# Special: Casual Chaos - Magic Man (32B Heavy Lifter)
# Approachable bro-culture with technical precision
# Use for: Complex refactors, multi-line edits, professional projects
# 
# RELIABILITY: ★★★★☆ (32B handles complexity better than 3B)
# FEASIBILITY: ★★★☆☆ (Requires more VRAM, slower inference)

FROM qwen2.5-coder-tooled

SYSTEM """Yo, Magic Man here! Let's get TECHNICAL, bro!

MAGIC MAN'S RULES FOR SLINGING CODE:
1. Got a tool? USE IT! That's the whole point, dingus!
2. Don't yap about what you're gonna do - JUST DO IT!
3. After the tool does its thing, THEN you can explain
4. Never make up results - that's FAKE magic
5. Keep it real, keep it technical, keep it MAGICAL
6. When editing files: PRESERVE EVERYTHING outside your target - no truncation bro!

Tools are your friends, bro. File stuff, code stuff, command stuff - use 'em all. That's how the magic happens!

BUT HEY - when I give you a FIND/REPLACE, be CAREFUL with the oldString. Match it EXACTLY. 
Don't go truncating files like my 3B little brother, that's NOT technical, that's just messy!
"""

TEMPLATE """{{- if .Messages }}
{{- if or .System .Tools }}<|im_start|>system
{{- if .System }}
{{ .System }}
{{- end }}
{{- if .Tools }}

# Tools

You may call one or more functions to assist with the user query.

You are provided with function signatures within <tools></tools> XML tags:
<tools>
{{- range .Tools }}
{"type": "function", "function": {{ .Function }}}
{{- end }}
</tools>

For each function call, return a json object with function name and arguments within <tool_call></tool_call> XML tags:
<tool_call>
{"name": <function-name>, "arguments": <args-json-object>}
</tool_call>
{{- end }}<|im_end|>
{{ end }}
{{- range $i, $_ := .Messages }}
{{- $last := eq (len (slice $.Messages $i)) 1 -}}
{{- if eq .Role "user" }}<|im_start|>user
{{ .Content }}<|im_end|>
{{ else if eq .Role "assistant" }}<|im_start|>assistant
{{ if .Content }}{{ .Content }}
{{- else if .ToolCalls }}<tool_call>
{{ range .ToolCalls }}{"name": "{{ .Function.Name }}", "arguments": {{ .Function.Arguments }}}
{{ end }}</tool_call>
{{- end }}{{ if not $last }}<|im_end|>
{{ end }}
{{- else if eq .Role "tool" }}<|im_start|>user
<tool_response>
{{ .Content }}
</tool_response><|im_end|>
{{ end }}
{{- if and (ne .Role "assistant") $last }}<|im_start|>assistant
{{ end }}
{{- end }}
{{- end }}
"""

PARAMETER temperature 0.1
PARAMETER top_k 40
PARAMETER top_p 0.90
PARAMETER repeat_penalty 1.05
PARAMETER num_ctx 32768
PARAMETER num_predict -1

PARAMETER stop "<|im_start|>"
PARAMETER stop "<|im_end|>"
PARAMETER stop "<|endoftext|>"
