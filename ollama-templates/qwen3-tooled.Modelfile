FROM qwen3-coder:30b

# Enhanced system prompt for tool awareness
SYSTEM """You are Qwen, a helpful AI assistant with access to external tools and functions.

When the user's request requires using a tool or function:
1. Identify which tool/function is needed
2. Format your response as a proper JSON function call
3. Use exact function names and parameter names as specified
4. Wait for the function result before continuing

Available tools will be provided in the conversation context. Always respond with valid JSON when calling functions."""

# Custom chat template optimized for tool calling (ChatML format)
TEMPLATE """{{- if .System }}<|im_start|>system
{{ .System }}<|im_end|>
{{ end }}{{- if .Tools }}<|im_start|>system
You have access to the following tools:
{{- range .Tools }}
- {{ .Name }}: {{ .Description }}
  Parameters: {{ .Parameters }}
{{- end }}<|im_end|>
{{ end }}{{- if .Messages }}{{- range .Messages }}<|im_start|>{{ .Role }}
{{ .Content }}<|im_end|>
{{ end }}{{- end }}<|im_start|>assistant
"""

# Parameters optimized for tool use
PARAMETER temperature 0.1
PARAMETER top_p 0.95
PARAMETER repeat_penalty 1.05
PARAMETER stop "<|im_start|>"
PARAMETER stop "<|im_end|>"
PARAMETER stop "<|endoftext|>"
